Ext.define('GF.controller.admin.settings.SettingsScreen', {
    extend: 'Ext.app.Controller',
    
    stores: ['members.Member'],
    
    views: [
        'admin.settings.SettingsScreen'
    ],
    
    init: function() {
        this.control({

        });
    }
});
